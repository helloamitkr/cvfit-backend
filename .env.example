# Local dev — copy to .env and source before running `make run`

PORT=8080
ENVIRONMENT=development
LOG_LEVEL=info

# AWS (only needed for S3/DynamoDB features; leave blank for local)
AWS_REGION=ap-south-1
S3_BUCKET=
DYNAMODB_TABLE_NAME=
SQS_QUEUE_URL=
RESUME_TEMPLATE_BUCKET=
RESUME_OUTPUT_BUCKET=
